import { Plugin } from 'vuepress/core';

interface TocPropsOptions {
    containerTag: string;
    containerClass: string;
    listClass: string;
    itemClass: string;
    linkTag: 'a' | 'RouteLink' | 'RouterLink';
    linkClass: string;
    linkActiveClass: string;
    linkChildrenActiveClass: string;
}

/**
 * Options for @vuepress/plugin-toc
 */
interface TocPluginOptions {
    /**
     * Specify the name of the TOC component
     *
     * @default 'Toc'
     */
    componentName?: string;
    /**
     * Override the default values of the `options` prop of the TOC component
     */
    defaultPropsOptions?: Partial<TocPropsOptions>;
}
declare const tocPlugin: ({ componentName, defaultPropsOptions, }?: TocPluginOptions) => Plugin;

export { type TocPluginOptions, type TocPropsOptions, tocPlugin };
